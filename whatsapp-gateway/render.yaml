services:
  - type: web
    name: firehub-whatsapp-gateway
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 8080
      - key: FIREHUB_WEBHOOK_URL
        value: https://firehubfood.com.br/api/webhook/whatsapp
